@{
    ViewData["Title"] = "New WeightSheet";
}

<a href="/WeightSheets" class="gm-module-bar gm-module-bar--warehouse gm-module-bar--link" title="Back to Weight Sheets">
    <i class="dx-icon dx-icon-arrowleft gm-module-bar__arrow"></i>
    <span class="gm-module-bar__icon gm-icon gm-icon--warehouse" aria-hidden="true"></span>
    <span class="gm-module-bar__label">NEW WEIGHTSHEET</span>
</a>

<!-- ── Step 1: Seed or Warehouse ─────────────────────────────────── -->
<div id="ltStep1">
    <h3 class="text-center mt-4 mb-3 fw-semibold" style="font-size:2rem;">Seed or Warehouse?</h3>
    <div class="row g-4 justify-content-center">
        <div class="col-12 col-lg-4">
            <a class="gm-tile gm-tile--link gm-tile--seed" href="#" data-lot-type="seed">
                <div class="gm-tile__header">
                    <div class="gm-tile__icon gm-tile__icon--bold gm-icon gm-icon--seed" aria-hidden="true"></div>
                    <h2 class="gm-tile__title">Seed</h2>
                </div>
                <p class="gm-tile__text">Seed receiving lot</p>
            </a>
        </div>
        <div class="col-12 col-lg-4">
            <a class="gm-tile gm-tile--link gm-tile--warehouse" href="#" data-lot-type="warehouse">
                <div class="gm-tile__header">
                    <div class="gm-tile__icon gm-tile__icon--bold gm-icon gm-icon--warehouse" aria-hidden="true"></div>
                    <h2 class="gm-tile__title">Warehouse</h2>
                </div>
                <p class="gm-tile__text">Warehouse receiving lot</p>
            </a>
        </div>
    </div>
</div>

<!-- ── Step 2: Grower Delivery or Transfer ───────────────────────── -->
<div id="ltStep2" hidden>
    <h3 class="text-center mt-4 mb-3 fw-semibold" style="font-size:2rem;">Select Weight Sheet Type</h3>
    <div class="row g-4 justify-content-center">
        <div class="col-12 col-lg-4">
            <a class="gm-tile gm-tile--link gm-tile--intake" href="#" data-ws-type="delivery">
                <div class="gm-tile__header">
                    <div class="gm-tile__icon gm-tile__icon--bold gm-icon gm-icon--delivery" aria-hidden="true"></div>
                    <h2 class="gm-tile__title">Grower Delivery</h2>
                </div>
                <p class="gm-tile__text">Inbound from a grower</p>
            </a>
        </div>
        <div class="col-12 col-lg-4">
            <a class="gm-tile gm-tile--link gm-tile--transfer" href="#" data-ws-type="transfer">
                <div class="gm-tile__header">
                    <div class="gm-tile__icon gm-tile__icon--bold gm-icon gm-icon--transfer gm-icon--flip" aria-hidden="true"></div>
                    <h2 class="gm-tile__title">Transfer</h2>
                </div>
                <p class="gm-tile__text">Inbound from another location</p>
            </a>
        </div>
    </div>
    <div class="text-center mt-3">
        <button type="button" class="btn btn-outline-secondary btn-sm" id="ltBackToStep1">Back</button>
    </div>
</div>

<!-- Authenticated user display -->
<div id="ltAuthInfo" class="text-center mt-3 text-muted" hidden></div>

<!-- ── PIN popup (Step 3) ────────────────────────────────────────── -->
<div class="modal fade" id="ltPinModal" tabindex="-1" aria-labelledby="ltPinModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-sm">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="ltPinModalLabel">Enter PIN</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p class="text-muted small mb-2">Enter your PIN to create a new weight sheet.</p>
                <div>
                    <label class="form-label">PIN <span class="text-danger">*</span></label>
                    <input type="text" class="form-control" id="ltPinInput"
                           inputmode="numeric" maxlength="10" placeholder="Enter PIN"
                           autocomplete="off" data-lpignore="true" data-1p-ignore="true"
                           data-form-type="other"
                           style="max-width:160px; -webkit-text-security:disc;" />
                </div>
                <div id="ltPinError" class="text-danger small mt-2" hidden></div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary" id="ltPinConfirmBtn">Continue</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script>
(function () {
    'use strict';
    var _lotType = null;   // 'seed' or 'warehouse'
    var _wsType = null;    // 'delivery' or 'transfer'
    var _pin = null;
    var _userName = null;
    var _pinModal = null;

    $(function () {
        _pinModal = new bootstrap.Modal(document.getElementById('ltPinModal'));

        // ── Step 1: Lot type selection ──────────────────────────────
        $('#ltStep1 [data-lot-type]').on('click', function (e) {
            e.preventDefault();
            _lotType = $(this).data('lot-type');
            $('#ltStep1').attr('hidden', '');
            $('#ltStep2').removeAttr('hidden');
        });

        // ── Step 2: Weight sheet type selection ─────────────────────
        $('#ltStep2 [data-ws-type]').on('click', function (e) {
            e.preventDefault();
            _wsType = $(this).data('ws-type');
            // Show PIN modal
            $('#ltPinInput').val('');
            $('#ltPinError').attr('hidden', '');
            _pinModal.show();
            setTimeout(function () { $('#ltPinInput').focus(); }, 500);
        });

        // ── Back button: Step 2 → Step 1 ────────────────────────────
        $('#ltBackToStep1').on('click', function () {
            _lotType = null;
            $('#ltStep2').attr('hidden', '');
            $('#ltStep1').removeAttr('hidden');
        });

        // ── Step 3: PIN validation ──────────────────────────────────
        $('#ltPinInput').on('keydown', function (e) {
            if (e.key === 'Enter') { e.preventDefault(); $('#ltPinConfirmBtn').click(); }
        });

        $('#ltPinConfirmBtn').on('click', function () {
            var pin = parseInt($('#ltPinInput').val(), 10);
            if (!pin || pin <= 0) {
                $('#ltPinError').text('A valid PIN is required.').removeAttr('hidden');
                return;
            }
            $.ajax({
                url: '/api/GrowerDelivery/ValidatePin?pin=' + pin,
                method: 'GET',
                dataType: 'json',
            })
            .done(function (data) {
                _pin = pin;
                _userName = data.UserName;
                _pinModal.hide();

                // Navigate to the correct page based on selections
                var url;
                if (_wsType === 'delivery') {
                    url = '/GrowerDelivery/NewWeightSheet?pin=' + _pin + '&lotType=' + _lotType;
                } else {
                    url = '/WeightSheets/TransferIn?pin=' + _pin + '&lotType=' + _lotType;
                }
                window.location.href = url;
            })
            .fail(function (xhr) {
                var msg = xhr.responseJSON && xhr.responseJSON.message
                    ? xhr.responseJSON.message : 'Invalid PIN.';
                $('#ltPinError').text(msg).removeAttr('hidden');
            });
        });

        // If modal dismissed without PIN, go back to step 2
        document.getElementById('ltPinModal').addEventListener('hidden.bs.modal', function () {
            if (!_pin) {
                // Stay on step 2, user can re-select
            }
        });
    });
})();
</script>
}
